package domain.enums;

public enum SpeechType {
    STATEMENT, // answer, claim (communicate a information), confirm, deny
    DIRECTIVE, // question, request information
    ACKNOWLEDGEMENT // greeting, thanking, apologize
}
